/**
 *  设计器配置信息
 */
export default {
    // 插件名称，对应plugins下的目录名称，如list对应plugins/list
    // 每个插件目录下方，必需有一个config文件，做插件定义，否则就不是插件
    pluginNames: ['api', 'table', 'form-combination']
    // , 'advanced-form', 'table-json', 'simple-form',
}
